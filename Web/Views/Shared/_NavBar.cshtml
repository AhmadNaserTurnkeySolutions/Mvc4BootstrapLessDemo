@{
  var current = Url.Current();
}
<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="brand" href="@Url.Home()">MVC4.less Demo</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li class="@(current == @Url.Home() ? "active" : "")"><a href="@Url.Home()">Home</a></li>
          <li class="@(current == @Url.GetStarted() ? "active" : "")"><a href="@Url.GetStarted()">Get Started</a></li>
          <li class="@(current == @Url.Scaffolding() ? "active" : "")"><a href="@Url.Scaffolding()">Scaffolding</a></li>
          <li class="@(current == @Url.BaseCss() ? "active" : "")"><a href="@Url.BaseCss()">Base CSS</a></li>
          <li class="@(current == @Url.Components() ? "active" : "")"><a href="@Url.Components()">Components</a></li>
          <li class="@(current == @Url.Javascript() ? "active" : "")"><a href="@Url.Javascript()">Javascript</a></li>
          <li class="@(current == @Url.Customize() ? "active" : "")"><a href="@Url.Customize()">Customize</a></li>
        </ul>
        
        @{ Html.RenderPartial("_LoginPartial"); }
      </div>
    </div>
  </div>
</div>